input_txt = input()
total = input_txt.split()
"""
파이썬에서 문자열을 빈칸(공백, 탭, 줄바꿈) 기준으로 자르려면 split() 메서드를 사용
인자 없이 text.split()을 호출하면 연속된 공백도 하나의 구분자로 처리하여 문자열을 단어 단위로 리스트화함

split(): 빈칸(스페이스, 탭, 엔터)을 기준으로 자름. 연속된 공백은 1개로 취급
split(' '): 인자를 넣으면 그 문자(공백) 하나를 기준으로만 자름
문자열 전처리: strip()으로 양끝 공백을 제거 후 split()을 사용하면 더 정확함
"""
print(len(total))
